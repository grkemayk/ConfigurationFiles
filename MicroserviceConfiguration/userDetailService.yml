server:
  port: 8600
spring:
  application:
    name: USER-DETAIL
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/koi
    password: Ga.4567.
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: none
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
eureka:
  client:
    serviceUrl:
      defaultZone: http://grkem:1234@localhost:8001/eureka/
    healthcheck:
      enabled: true
logging:
  config: http://localhost:8000/native/native/main/LogConfiguration/UserDetailServiceLogback.xml
userDetail:
  defaultStar: 2.5




