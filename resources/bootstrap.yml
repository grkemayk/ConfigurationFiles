spring:
  application:
    name: authServer
  cloud:
    config:
      profile: native
      uri: ${CONFIG_SERVER}
      username: ${CONFIG_SERVER_USERNAME}
      password: ${CONFIG_SERVER_PASSWORD}
encrypt:
  key: ${CONFIG_SERVER_ENCRYPT_KEY}
logging:
  config:

